# ascii-video
Video to ASCII in cmd

# installation: 
install opencv-python
```bash
pip install opencv-python==4.9.0.80
```

add to path

# Usage
```bash
ascii -v C:\Path\To\video.mp4 (You can also drag and drop the video file into the terminal to automatically insert its path)
```
